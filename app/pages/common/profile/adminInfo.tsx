'use client'

// AdminInfo component - removed system stats and recent activity
// These features are available in dedicated pages (Analytics, Audit Logs)

export default function AdminInfo() {
  // No additional info to display for admin in profile page
  return null
}
